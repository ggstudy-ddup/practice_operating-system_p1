#ifndef __INCLUDE_STRNUM_H__
#define __INCLUDE_STRNUM_H__

#include <types.h>

/* int to ascii digital */
int itoa(int i, char * buf);

/* unsigned int to ascii hexidecimal */
int uitox(int i, char * buf);

/* long to ascii */
// char * ltoa(long i, 

#endif /* !__INCLUDE_STRNUM_H__ */
